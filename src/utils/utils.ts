/**
 * A function which multiplies two numbers
 * @param param1 number 1
 * @param param2 number 2
 * @returns the calculation of the two numbers
 */
export const helperFunction = (param1: number, param2: number): number =>
    param1 * param2;
